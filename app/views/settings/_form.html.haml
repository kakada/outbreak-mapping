= simple_form_for @setting, url: setting_path do |f|
  = f.input :last_updated

  .actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to "Cancel", setting_path, class: 'btn btn-secondary'